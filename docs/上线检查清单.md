# ShellWard 上线检查清单

## 生产可用性评估

| 项目 | 状态 | 说明 |
|------|------|------|
| 核心功能 | ✅ | 8 层防御全部实现并测试通过 |
| 集成测试 | ✅ | 38 项 OpenClaw 集成测试 |
| 边界测试 | ✅ | 42 项边界/绕过/性能测试 |
| SDK 测试 | ✅ | 32 项独立 SDK 测试 |
| 文档一致性 | ✅ | README 与实现一致（审计模式） |
| Windows 兼容 | ✅ | 使用 os.homedir() |
| 版本统一 | ✅ | package.json / openclaw.plugin.json 均为 0.5.0 |

## 上线前建议

1. **npm 发布**
   ```bash
   npm run test          # 确保全部通过
   npm publish           # 需先 npm login
   ```

2. **OpenClaw 安装**
   - 已发布: `openclaw plugins install shellward`
   - 未发布: 使用 install.sh 从 git 安装

3. **配置建议**
   - 生产环境: `mode: "enforce"`
   - 试运行: `mode: "audit"` 观察日志后再切换

## 已知限制

- 审计日志路径: `~/.openclaw/shellward/audit.jsonl`（可通过环境变量 HOME 间接影响）
- 非 OpenClaw 环境使用 SDK 需支持 TypeScript 运行时（如 tsx、ts-node）或预编译

## 结论

**可以上线使用。** 112 项测试全通过，文档与实现一致，Windows 兼容，架构清晰。
